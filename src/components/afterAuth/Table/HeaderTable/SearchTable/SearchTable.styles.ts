import styled from "styled-components"

const InputFormWrapper = styled.div`
  display: flex;
  position: relative;
  font-size: 14px;
  margin-top: 20px;
  margin-left: 20px;
  margin-bottom: 20px;
`


const StyledSearchTable = { InputFormWrapper }

export default StyledSearchTable
