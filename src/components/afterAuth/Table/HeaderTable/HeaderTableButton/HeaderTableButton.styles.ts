import styled from "styled-components"

const HeaderTableButtonWrapper = styled.div`
  margin-top: 20px;
  margin-right: 30px;
  margin-bottom: 20px;
`
const StyledHeaderTableButton = { HeaderTableButtonWrapper }

export default StyledHeaderTableButton
