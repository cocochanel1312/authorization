import styled from "styled-components"

const LayoutWrapper = styled.div`
  height: 100%;
  width: 100%;
  background-color: black;
`

const StylesSidebar = { LayoutWrapper }

export default StylesSidebar
